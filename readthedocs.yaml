version: 2

build:
  os: ubuntu-22.04
  tools:
    python: "3.11"
  apt_packages:
    - pandoc                        # Required by nbsphinx to convert notebooks

sphinx:
  configuration: docs/source/conf.py

python:
  install:
    - requirements: docs/requirements.txt  # Installs everything via pyproject.toml
